export type MetaData = {
    title: string,
    description: string,
    keywords?: string,
    canonicalUrl?: string,
    ampHTML?: string,
    imageUrl?: string,
    noIndex?: boolean,
    author?: string,
}